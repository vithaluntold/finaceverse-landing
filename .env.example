# FinACEverse Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git!

# ============================================
# NODE ENVIRONMENT
# ============================================
NODE_ENV=production
PORT=5000

# ============================================
# JWT SECURITY (REQUIRED)
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be at least 32 characters in production!
JWT_SECRET=your-64-character-random-secret-change-in-production

# JWT Refresh Token Secret (separate from access token)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_REFRESH_SECRET=your-64-character-refresh-secret-change-in-production

# ============================================
# ENCRYPTION (REQUIRED - AES-256-GCM)
# ============================================
# Master encryption key for data at rest
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# CRITICAL: If you lose this key, encrypted data cannot be recovered!
ENCRYPTION_KEY=your-64-character-encryption-key-change-in-production

# ============================================
# CSRF PROTECTION (REQUIRED)
# ============================================
# Secret for CSRF token signing
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CSRF_SECRET=your-64-character-csrf-secret-change-in-production

# ============================================
# ADMIN SETUP (REQUIRED)
# ============================================
# Secret key for creating admin users
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('base64'))"
ADMIN_SECRET_KEY=your-strong-admin-secret-key-change-in-production

# ============================================
# CORS CONFIGURATION (REQUIRED IN PRODUCTION)
# ============================================
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://www.finaceverse.io,https://finaceverse.io

# ============================================
# DATABASE (REQUIRED)
# ============================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://localhost:5432/finaceverse_analytics
# For Railway Postgres (automatically provided):
# DATABASE_URL=postgresql://user:password@host:port/database

# Redis connection string (optional but recommended for caching)
# Format: redis://username:password@host:port
REDIS_URL=redis://localhost:6379
# For Redis Cloud:
# REDIS_URL=redis://default:password@redis-12345.cloud.redislabs.com:12345

# ============================================
# GOOGLE APIS (OPTIONAL)
# ============================================
# Google Cloud Platform API Key (for PageSpeed Insights)
GOOGLE_API_KEY=your_google_cloud_api_key_here

# OAuth 2.0 Credentials (for Google Search Console API)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# ============================================
# MAILGUN (OPTIONAL)
# ============================================
# Newsletter/Email service
MAILGUN_API_KEY=your_mailgun_api_key_here
MAILGUN_DOMAIN=finaceverse.io
MAILGUN_MAILING_LIST=newsletter@finaceverse.io

# Example:
# MAILGUN_API_KEY=key-1234567890abcdef1234567890abcdef
# MAILGUN_DOMAIN=mg.finaceverse.io
# MAILGUN_MAILING_LIST=newsletter@mg.finaceverse.io

# ============================================
# GOOGLE ANALYTICS (FRONTEND)
# ============================================
# GA4 Measurement ID
REACT_APP_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================
# API URL (FRONTEND)
# ============================================
# Backend API URL for frontend to connect
REACT_APP_API_URL=http://localhost:5000/api
# Production:
# REACT_APP_API_URL=https://www.finaceverse.io/api

# ============================================
# IP GEOLOCATION (OPTIONAL)
# ============================================
# Uses ipapi.co free tier by default (1000 req/day)
# IPAPI_KEY=your_ipapi_key_for_higher_rate_limits

# ============================================
# DEPLOYMENT PLATFORM (AUTOMATIC)
# ============================================
# Railway.app or other platform-specific variables
# Usually set automatically by the platform
# RAILWAY_ENVIRONMENT=production
# RAILWAY_GIT_COMMIT_SHA=
# RAILWAY_GIT_BRANCH=

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER use default values in production
# 2. Generate strong random secrets using crypto
# 3. Keep this file in .gitignore
# 4. Use different secrets for dev and production
# 5. Rotate secrets periodically (quarterly)
# 6. Store production secrets in deployment platform's secret manager
# 7. Minimum password length: 12 characters
# 8. JWT tokens expire after 24 hours

